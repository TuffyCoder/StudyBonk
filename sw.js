// Generated by scripts/build.py — StudyBonk service worker.
// Precaches the app shell; pages are cached on first visit (runtime caching).
const CACHE = "studybonk-20260906151613";
const PRECACHE = ["/StudyBonk/", "/StudyBonk/learn/", "/StudyBonk/flashcards/", "/StudyBonk/quiz/", "/StudyBonk/focus/", "/StudyBonk/ai/", "/StudyBonk/dashboard/", "/StudyBonk/manifest.webmanifest", "/StudyBonk/assets/css/style.css", "/StudyBonk/assets/fonts/fonts.css", "/StudyBonk/assets/js/storage.js", "/StudyBonk/assets/js/components.js", "/StudyBonk/assets/js/gamification.js", "/StudyBonk/assets/js/nav.js", "/StudyBonk/assets/js/theme.js", "/StudyBonk/assets/js/theme-boot.js", "/StudyBonk/assets/js/study-data.js", "/StudyBonk/assets/js/flashcards.js", "/StudyBonk/assets/js/quiz.js", "/StudyBonk/assets/js/focus.js", "/StudyBonk/assets/js/dashboard.js", "/StudyBonk/assets/js/ai.js", "/StudyBonk/assets/js/bonk-model.js", "/StudyBonk/assets/img/mascot.svg", "/StudyBonk/assets/img/logo.svg", "/StudyBonk/assets/img/favicon.svg", "/StudyBonk/assets/img/og-image.png", "/StudyBonk/assets/img/creator-avatar.jpg", "/StudyBonk/assets/fonts/Baloo2-normal-800.woff2", "/StudyBonk/assets/fonts/Nunito-normal-400.woff2", "/StudyBonk/assets/fonts/Nunito-normal-700.woff2"];

self.addEventListener("install", (e) => {
  e.waitUntil(
    caches.open(CACHE)
      .then((c) => Promise.allSettled(PRECACHE.map((u) => c.add(u))))
      .then(() => self.skipWaiting())
  );
});

self.addEventListener("activate", (e) => {
  e.waitUntil(
    caches.keys().then((keys) => Promise.all(keys.filter((k) => k !== CACHE).map((k) => caches.delete(k))))
      .then(() => self.clients.claim())
  );
});

self.addEventListener("fetch", (e) => {
  const url = new URL(e.request.url);
  if (e.request.method !== "GET" || url.origin !== location.origin) return; // never touch cross-origin (model CDN etc.)
  e.respondWith(
    caches.match(e.request, { ignoreSearch: true }).then((hit) => {
      const fetching = fetch(e.request).then((res) => {
        if (res && res.ok) {
          const copy = res.clone();
          caches.open(CACHE).then((c) => c.put(e.request, copy));
        }
        return res;
      });
      return hit ? Promise.resolve(hit) : fetching;
    }).catch(() =>
      caches.match("/").then((shell) => shell || new Response("Offline", { status: 503, headers: { "Content-Type": "text/plain" } }))
    )
  );
});
